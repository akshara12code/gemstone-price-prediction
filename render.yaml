services:
  # Flask Application ONLY (no Airflow on Render)
  - type: web
    name: gemstone-flask
    env: docker
    dockerfilePath: ./Dockerfile.flask
    region: oregon
    plan: free
    healthCheckPath: /